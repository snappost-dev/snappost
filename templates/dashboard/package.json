{
  "name": "snappost-dashboard",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "wrangler pages dev --local -- astro dev --port 4322",
    "dev:local": "npm run build && wrangler pages dev dist --local --persist-to ../../.snappost-d1-local",
    "build": "astro build",
    "preview": "wrangler pages dev dist"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^11.0.0",
    "@astrojs/tailwind": "^5.1.0",
    "@types/bcryptjs": "^2.4.6",
    "astro": "^4.15.0",
    "bcryptjs": "^2.4.3",
    "daisyui": "^4.12.24",
    "marked": "^12.0.0",
    "tailwindcss": "^3.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "wrangler": "^3.57.0"
  }
}
